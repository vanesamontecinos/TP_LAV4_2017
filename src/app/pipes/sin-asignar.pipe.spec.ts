import { SinAsignarPipe } from './sin-asignar.pipe';

describe('SinAsignarPipe', () => {
  it('create an instance', () => {
    const pipe = new SinAsignarPipe();
    expect(pipe).toBeTruthy();
  });
});
